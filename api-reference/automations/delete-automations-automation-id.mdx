---
title: "DELETE /automations/{automation_id}"
description: "Captured contract for DELETE /automations/{automation_id}."
api: "DELETE /automations/{automation_id}"
public: true
---

This internal review page records the behavior captured for DELETE /automations/{automation_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Delete automation API.

Endpoint:
DELETE: api/automations/{automation_id}

Response:

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

