---
title: "Delete a call history"
description: "Captured contract for DELETE /calls/{number_id}/history."
api: "DELETE /calls/{number_id}/history"
public: true
---

This page documents the behavior captured for DELETE /calls/{number_id}/history in the Amplify API source materials.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
    Endpoint:
    DELETE: api/calls/{number_id}/history
    
    Response:
    
    image.png
```

<Note>Only behavior explicitly present in the captured endpoint material is shown. Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

