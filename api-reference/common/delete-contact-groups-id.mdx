---
title: "DELETE /contact_groups/{id}"
description: "Captured contract for DELETE /contact_groups/{id}."
api: "DELETE /contact_groups/{id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /contact_groups/{id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Endpoint
DELETE api/contact_groups/{id}

Success Response (204)
image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

