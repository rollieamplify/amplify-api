---
title: "DELETE /contacts/note/{note_id}"
description: "Captured contract for DELETE /contacts/note/{note_id}."
api: "DELETE /contacts/note/{note_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /contacts/note/{note_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Delete contact note API:

Endpoint:
POST: api/contacts/note/{note_id}


Response:
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

