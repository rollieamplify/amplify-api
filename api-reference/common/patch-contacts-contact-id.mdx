---
title: "PATCH /contacts/{contact_id}"
description: "Captured contract for PATCH /contacts/{contact_id}."
api: "PATCH /contacts/{contact_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for PATCH /contacts/{contact_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Add extra details or attach a file…
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

