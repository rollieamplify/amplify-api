---
title: "List automation segments"
description: "List the conditions and actions supported by an automation trigger."
openapi: "POST /v1/automations/segments"
---

Returns the condition segments, actions, and comparison operators available for the specified `trigger_type`.

The response can be large and varies by trigger. The example is shortened to show its structure; use the live response when building an automation configuration.
