---
title: "Mark a fax as done"
description: "Mark an inbound or outbound fax as done."
openapi: "POST /v1/faxes/mark-as-done"
---

Provide the required fax `job_id` and set `type` to `inbound` or `outbound`.
