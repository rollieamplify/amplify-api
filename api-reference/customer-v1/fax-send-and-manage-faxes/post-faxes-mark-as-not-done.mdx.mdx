---
title: "Mark a fax as not done"
description: "Mark an inbound or outbound fax as not done."
openapi: "POST /v1/faxes/mark-as-not-done"
---

Provide the required fax `job_id` and set `type` to `inbound` or `outbound`.
