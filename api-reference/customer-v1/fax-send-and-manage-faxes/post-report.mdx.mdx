---
title: "Generate a fax report"
description: "Customer API v1 endpoint."
openapi: "POST /v1/report"
hidden: true
noindex: true
---

Generate a fax report. Authenticate with a Bearer access token unless the endpoint is the authentication token exchange.

<Warning>
This endpoint remains under review. Testing with and without a date range returned `404 Not Found` with an opaque response, so a successful public contract has not been confirmed.
</Warning>
