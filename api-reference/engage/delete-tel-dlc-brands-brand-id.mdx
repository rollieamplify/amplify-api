---
title: "DELETE /tel_dlc/brands/{brand_id}"
description: "Captured contract for DELETE /tel_dlc/brands/{brand_id}."
api: "DELETE /tel_dlc/brands/{brand_id}"
public: true
---

This page documents the behavior captured for DELETE /tel_dlc/brands/{brand_id} in the Amplify API source materials.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
    Endpoint:
    DELETE: api/tel_dlc/brands/{brand_id}
    
    Response:
    
    image.png
```

<Note>Only behavior explicitly present in the captured endpoint material is shown. Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

