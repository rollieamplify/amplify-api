---
title: "POST /esign_documents/{document_id}/send"
description: "Captured contract for POST /esign_documents/{document_id}/send."
api: "POST /esign_documents/{document_id}/send"
public: true
---

This internal review page records the behavior captured for POST /esign_documents/{document_id}/send.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




POST : api/esign_documents/{document_id}/send




Description :

-> Sends a draft document to all configured recipients. 
-> Deducts 1 eSign credit on first send, updates document status to sent, creates status logs, refreshes recipient document URLs, and dispatches background email jobs.




 Authentication :
  Include the Bearer Token in the request header. 




access_token: YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

