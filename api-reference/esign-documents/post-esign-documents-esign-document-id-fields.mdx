---
title: "POST /esign_documents/{esign_document_id}/fields"
description: "Captured contract for POST /esign_documents/{esign_document_id}/fields."
api: "POST /esign_documents/{esign_document_id}/fields"
public: true
---

This internal review page records the behavior captured for POST /esign_documents/{esign_document_id}/fields.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




POST : api/esign_documents/{esign_document_id}/fields




Description :

-> Creates one or more fields on a document. 
-> Always pass fields as an array. 
-> User must have edit access to the document.




 Authentication :
  Include the Bearer Token in the request header. 




Authorization: Bearer YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

