---
title: "Delete a Collect template field rule"
description: "Captured contract for DELETE /esign_templates/{template_id}/field_rule/{field_rule_id}."
api: "DELETE /esign_templates/{template_id}/field_rule/{field_rule_id}"
public: true
---

This internal review page records the behavior captured for DELETE /esign_templates/{template_id}/field_rule/{field_rule_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
This API is used for delete a document field rule.

Endpoint: 
DELETE: api/esign_templates/{template_id}/field_rule/{field_rule_id}

Response:
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

