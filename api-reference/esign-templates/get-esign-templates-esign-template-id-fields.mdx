---
title: "GET /esign_templates/{esign_template_id}/fields"
description: "Captured contract for GET /esign_templates/{esign_template_id}/fields."
api: "GET /esign_templates/{esign_template_id}/fields"
public: true
---

This internal review page records the behavior captured for GET /esign_templates/{esign_template_id}/fields.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




GET : api/esign_templates/{esign_template_id}/fields




Description :




-> Returns all fields placed on the template.




 Authentication :
  Include the Bearer Token in the request header. 




access_token: YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

