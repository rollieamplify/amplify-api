---
title: "GET /esign_templates/{template_id}"
description: "Captured contract for GET /esign_templates/{template_id}."
api: "GET /esign_templates/{template_id}"
public: true
---

This internal review page records the behavior captured for GET /esign_templates/{template_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




GET : api/esign_templates/{template_id}




Description :




Returns full template details including signers, files, variables, fields, field rules, theme, folder, labels, collaborators, and config.

 

Authentication :
  Include the Bearer Token in the request header. 




access_token: YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

