---
title: "PATCH /esign_templates/{esign_template_id}/variables/{template_variable_id}"
description: "Captured contract for PATCH /esign_templates/{esign_template_id}/variables/{template_variable_id}."
api: "PATCH /esign_templates/{esign_template_id}/variables/{template_variable_id}"
public: true
---

This internal review page records the behavior captured for PATCH /esign_templates/{esign_template_id}/variables/{template_variable_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




PATCH : api/esign_templates/{esign_template_id}/variables/{template_variable_id}




Description :




Updates one or more fields on a template variable. Only fields sent in the body are updated.




 Authentication :
  Include the Bearer Token in the request header. 




Authorization: Bearer YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

