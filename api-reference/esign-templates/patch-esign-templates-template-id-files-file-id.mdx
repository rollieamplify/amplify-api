---
title: "PATCH /esign_templates/{template_id}/files/{file_id}"
description: "Captured contract for PATCH /esign_templates/{template_id}/files/{file_id}."
api: "PATCH /esign_templates/{template_id}/files/{file_id}"
public: true
---

This internal review page records the behavior captured for PATCH /esign_templates/{template_id}/files/{file_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :

PATCH : api/esign_templates/{template_id}/files/{file_id}




Description :

-> Updates a multi-page file segment on a template (name, page range, page count). 
-> Recalculates template total page_count when pages change.




 Authentication :
  Include the Bearer Token in the request header. 




Authorization: Bearer YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

