---
title: "PATCH /esign_templates/{template_id}/theme"
description: "Captured contract for PATCH /esign_templates/{template_id}/theme."
api: "PATCH /esign_templates/{template_id}/theme"
public: true
---

This internal review page records the behavior captured for PATCH /esign_templates/{template_id}/theme.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
EndPoint :
PATCH : api/esign_templates/{template_id}/theme

Description :
 Updates the theme configuration for a templates.

 Authentication :
  Include the Bearer Token in the request header. 
Authorization: Bearer YOUR_ACCESS_TOKEN


Request Body (JSON)
Field | Type | Description
----------------------------------------------------------------
 font | string | Optional. Font family.
 font_size | integer | Optional. Font size.
 capitalization | string | Optional. Text capitalization style.
 line_spacing | float | Optional. Line spacing value.
 style | string | Optional. Font style.
 color | string | Optional. Text color.
 opacity | float | Optional. Text opacity.
 image_url | string | Optional. Background image URL.
 image_size | string | Optional. Background image size.
 image_position | string | Optional. Background image position.
 image_repeating | boolean | Optional. Whether background repeats.
 image_opacity | float | Optional. Background image opacity.
 applied_to_page | integer | Optional. Page number where theme applies. 

Request :
{
    "text": {
        "font": "Arial",
        "size": "18px",
        "style": [],
        "color": "#000000",
        "capitalization": "unset",
        "line_spacing": 2
    },
    "background": {
        "color": "#FFFFFF",
        "opacity": 100
    },
    "background_image": {
        "url": "",
        "size": "cover",
        "position": "left_top",
        "is_repeating": false,
        "opacity": 100
    },
    "applied_to_page": "all_pages"
}

Response :
{
    "data": {
        "id": 2326,
        "text": {
            "font": "Arial",
            "size": "18px",
            "style": [],
            "capitalization": "unset",
            "line_spacing": "2.00",
            "color": "#000000"
        },
        "background": {
            "color": "#FFFFFF",
            "opacity": 100
        },
        "background_image": {
            "url": "",
            "size": "cover",
            "position": "left_top",
            "is_repeating": false,
            "opacity": 100
        },
        "applied_to_page": "all_pages"
    },
    "meta": {
        "request_id": "d6e79c8f-5c4e-4bc3-8179-03b288b18b8f"
    }
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

