---
title: "POST /esign_templates/{template_id}/theme"
description: "Captured contract for POST /esign_templates/{template_id}/theme."
api: "POST /esign_templates/{template_id}/theme"
public: true
---

This internal review page records the behavior captured for POST /esign_templates/{template_id}/theme.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
EndPoint :
 POST : api/esign_templates/{template_id}/theme

Description :
Creates or updates the theme configuration for a template.
This endpoint acts as an upsert operation for template theme settings. 



 Authentication :
  Include the Bearer Token in the request header. 
Authorization: Bearer YOUR_ACCESS_TOKEN


Request Body (JSON)
Field | Type | Description
----------------------------------------------------------------
 font | string | Optional. Font family.
 font_size | integer | Optional. Font size.
 capitalization | string | Optional. Text capitalization style.
 line_spacing | float | Optional. Line spacing value.
 style | string | Optional. Font style (e.g., bold, italic).
 color | string | Optional. Text color.
 opacity | float | Optional. Text opacity.
 image_url | string | Optional. Background image URL.
 image_size | string | Optional. Background image size.
 image_position | string | Optional. Background image position.
 image_repeating | boolean | Optional. Whether background repeats.
 image_opacity | float | Optional. Background image opacity.
 applied_to_page | integer | Optional. Page number where theme applies. 



Request :
{
    "text": {
        "font": "Lora",
        "size": "10px",
        "style": [],
        "color": "#000000",
        "capitalization": "unset",
        "line_spacing": 1
    },
    "background": {
        "color": "#FFFFFF",
        "opacity": 100
    },
    "background_image": {
        "url": "",
        "size": "cover",
        "position": "left_top",
        "is_repeating": false,
        "opacity": 100
    },
    "applied_to_page": "all_pages"
}

Response :
{
    "data": {
        "id": 2326,
        "text": {
            "font": "Lora",
            "size": "10px",
            "style": [],
            "capitalization": "unset",
            "line_spacing": "1.00",
            "color": "#000000"
        },
        "background": {
            "color": "#FFFFFF",
            "opacity": 100
        },
        "background_image": {
            "url": "",
            "size": "cover",
            "position": "left_top",
            "is_repeating": false,
            "opacity": 100
        },
        "applied_to_page": "all_pages"
    },
    "meta": {
        "request_id": "87f83b45-cb84-423b-8291-3c700351b347"
    }
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

