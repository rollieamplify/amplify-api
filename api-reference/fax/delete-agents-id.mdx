---
title: "DELETE /agents/{id}"
description: "Captured contract for DELETE /agents/{id}."
api: "DELETE /agents/{id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /agents/{id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Delete Agent Details.

Endpoint
DELETE : api/agents/{id}

Response

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

