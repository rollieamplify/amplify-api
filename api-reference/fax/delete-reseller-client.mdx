---
title: "DELETE /reseller/client"
description: "Captured contract for DELETE /reseller/client."
api: "DELETE /reseller/client"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /reseller/client.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Delete Client API.

Endpoint :
DELETE - api/reseller/client/{client_id}

Response:

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

