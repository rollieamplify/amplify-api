---
title: "DELETE /schedule/report/{report_id}"
description: "Captured contract for DELETE /schedule/report/{report_id}."
api: "DELETE /schedule/report/{report_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /schedule/report/{report_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Endpoint:
DELETE: api/schedule/report/1

Response:
image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

