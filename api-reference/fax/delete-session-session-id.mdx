---
title: "DELETE /session/{session_id}"
description: "Captured contract for DELETE /session/{session_id}."
api: "DELETE /session/{session_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /session/{session_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Session Delete API.

Endpoint
DELETE : api/session/{session_id}

Response

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

