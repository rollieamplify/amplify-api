---
title: "DELETE /shortcuts/{shortcut_id}"
description: "Captured contract for DELETE /shortcuts/{shortcut_id}."
api: "DELETE /shortcuts/{shortcut_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /shortcuts/{shortcut_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Delete shortcut API.

Endpoint:
DELETE: api/shortcuts/{shortcut_api}

Response:

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

