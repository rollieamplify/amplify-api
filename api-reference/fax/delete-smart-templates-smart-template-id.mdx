---
title: "DELETE /smart_templates/{smart_template_id}"
description: "Captured contract for DELETE /smart_templates/{smart_template_id}."
api: "DELETE /smart_templates/{smart_template_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /smart_templates/{smart_template_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Endpoint:
DELETE: api/smart_templates/{smart_template_id}

Response: 204
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

