---
title: "DELETE /white_list/{id}"
description: "Captured contract for DELETE /white_list/{id}."
api: "DELETE /white_list/{id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /white_list/{id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Endpoint:
DELETE: api/white_list/{id}

Response:

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

