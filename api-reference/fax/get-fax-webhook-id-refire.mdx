---
title: "GET /fax/{webhook_id}/refire"
description: "Captured contract for GET /fax/{webhook_id}/refire."
api: "GET /fax/{webhook_id}/refire"
public: true
---

This internal review page records the behavior captured for GET /fax/{webhook_id}/refire.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Refire Existing Webhook

Endpoint:
GET: api/fax/{webhook_id}/refire
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

