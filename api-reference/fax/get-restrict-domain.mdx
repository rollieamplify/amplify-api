---
title: "GET /restrict_domain"
description: "Captured contract for GET /restrict_domain."
api: "GET /restrict_domain"
groups: ["internal"]
---

This internal review page records the behavior captured for GET /restrict_domain.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Get list of restricted domain.

Endpoint:
GET: api/restrict_domain

Response:
{
    "data": [
        {
            "id": 3,
            "domain": "gmail.com",
            "account_id": 285
        },
        {
            "id": 4,
            "domain": "moontechnolabs.com",
            "account_id": 285
        }
    ],
    "meta": {
        "request_id": "543c8ac3-2ec4-4b06-bca8-2d1b348cf577"
    }
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

