---
title: "GET /user_configuration"
description: "Captured contract for GET /user_configuration."
api: "GET /user_configuration"
groups: ["internal"]
---

This internal review page records the behavior captured for GET /user_configuration.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
List User Configurations API.

EndPoint
GET: https://beta-api.amplify.xyz/api/user_configuration
Response
{
    "data": {
        "domain_restrication": true,
        "team_auth_type": 2 // 0 = OFF, 1= SMS, 2-GA 
    },
    "meta": {
        "request_id": "93063cf4-e44e-4698-a34a-0c59d34b4172"
    }
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

