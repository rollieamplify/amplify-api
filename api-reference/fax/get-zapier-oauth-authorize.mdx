---
title: "GET /zapier/oauth/authorize"
description: "Captured contract for GET /zapier/oauth/authorize."
api: "GET /zapier/oauth/authorize"
groups: ["internal"]
---

This internal review page records the behavior captured for GET /zapier/oauth/authorize.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Zapier OAuth 2 implementation.
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

