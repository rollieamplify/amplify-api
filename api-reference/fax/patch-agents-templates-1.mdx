---
title: "PATCH /agents/templates/1"
description: "Captured contract for PATCH /agents/templates/1."
api: "PATCH /agents/templates/1"
groups: ["internal"]
---

This internal review page records the behavior captured for PATCH /agents/templates/1.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Add extra details or attach a file…
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

