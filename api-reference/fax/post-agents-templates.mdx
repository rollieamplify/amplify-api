---
title: "POST /agents/templates"
description: "Captured contract for POST /agents/templates."
api: "POST /agents/templates"
groups: ["internal"]
---

This internal review page records the behavior captured for POST /agents/templates.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Add extra details or attach a file…
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

