---
title: "POST /email_to_fax/allowed_domain"
description: "Captured contract for POST /email_to_fax/allowed_domain."
api: "POST /email_to_fax/allowed_domain"
groups: ["internal"]
---

This internal review page records the behavior captured for POST /email_to_fax/allowed_domain.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
- Create Allowed Domain

Endpoint:
POST: /api/email_to_fax/allowed_domain
Body:
{
  "domain": "example.com"
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

