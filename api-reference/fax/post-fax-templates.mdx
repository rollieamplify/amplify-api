---
title: "POST /fax_templates"
description: "Captured contract for POST /fax_templates."
api: "POST /fax_templates"
groups: ["internal"]
---

This internal review page records the behavior captured for POST /fax_templates.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Add extra details or attach a file…
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

