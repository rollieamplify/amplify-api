---
title: "POST /reseller_mode"
description: "Captured contract for POST /reseller_mode."
api: "POST /reseller_mode"
groups: ["internal"]
---

This internal review page records the behavior captured for POST /reseller_mode.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Update Reseller Mode


Endpoint :
POST /api/reseller_mode

Description :
Enable or disable reseller mode for the authenticated account’s branding row.

Request Body :

Field

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

