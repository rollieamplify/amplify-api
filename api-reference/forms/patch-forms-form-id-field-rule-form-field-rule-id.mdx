---
title: "PATCH /forms/{form_id}/field_rule/{form_field_rule_id}"
description: "Captured contract for PATCH /forms/{form_id}/field_rule/{form_field_rule_id}."
api: "PATCH /forms/{form_id}/field_rule/{form_field_rule_id}"
public: true
---

This internal review page records the behavior captured for PATCH /forms/{form_id}/field_rule/{form_field_rule_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




PATCH : api/forms/{form_id}/field_rule/{form_field_rule_id}




Description :

-> Updates an existing conditional field rule. 
-> Can update name, condition_value, and/or actions array.




 Authentication :
  Include the Bearer Token in the request header. 




access_token: YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

