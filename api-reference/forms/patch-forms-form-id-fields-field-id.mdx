---
title: "PATCH /forms/{form_id}/fields/{field_id}"
description: "Captured contract for PATCH /forms/{form_id}/fields/{field_id}."
api: "PATCH /forms/{form_id}/fields/{field_id}"
public: true
---

This internal review page records the behavior captured for PATCH /forms/{form_id}/fields/{field_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
End Point :




PATCH : api/forms/{form_id}/fields/{field_id}




Description :

-> Updates a single form field. 
-> Same logic as bulk update but returns a single field object in the response.




 Authentication :
  Include the Bearer Token in the request header. 




access_token: YOUR_ACCESS_TOKEN




Path Parameters:




Param

	

Type

	

Required
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

