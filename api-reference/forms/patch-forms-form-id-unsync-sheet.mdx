---
title: "PATCH /forms/{form_id}/unsync_sheet"
description: "Captured contract for PATCH /forms/{form_id}/unsync_sheet."
api: "PATCH /forms/{form_id}/unsync_sheet"
public: true
---

This internal review page records the behavior captured for PATCH /forms/{form_id}/unsync_sheet.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Endpoint: 
PATCH: api/forms/{form_id}/unsync_sheet


Response:
{
    "data":[],
    "meta": {}
}
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

