---
title: "POST /{fax_uuid}/labels/{label_id}"
description: "Captured contract for POST /{fax_uuid}/labels/{label_id}."
api: "POST /{fax_uuid}/labels/{label_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for POST /{fax_uuid}/labels/{label_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Remove Label from fax

Endpoint:
POST: api/01KDSF33N1C71NWJXM5H3KHWX5/labels/25
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

