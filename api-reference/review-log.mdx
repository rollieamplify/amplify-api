---
title: "API documentation review log"
description: "Open questions and source inconsistencies found while documenting the Amplify API."
groups: ["internal"]
---

# API documentation review log

This internal page records unresolved differences in the Basecamp endpoint materials. It is not part of the public API navigation.

## Authentication

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `POST /v1/auth` | Production and development base URLs, the success response, and the invalid-key `401` response are confirmed. Responses for a missing key and other authentication failures, refresh behavior, and scopes remain undocumented. | The public authentication guide uses the production endpoint and documents the API-key exchange, Bearer usage, and confirmed invalid-key response. | Confirm refresh behavior, scopes, remaining error responses, and formal OAuth 2.0 status. |

## Fax API

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `POST /v1/faxes/send` | Scheduling restrictions beyond ISO 8601 remain unknown. Missing and invalid Bearer-token responses and validation-error responses were skipped for later confirmation. The expired-token `401` response is confirmed. Permissions, rate limits, and idempotency behavior are also undocumented. | The public reference documents the confirmed JSON request, required fields, enums, template behavior, successful `200` response, and expired-token response. It does not expose unconfirmed restrictions or errors. | Confirm scheduling restrictions, remaining protected-endpoint errors, validation errors, permissions, rate limits, and idempotency behavior. |
| `POST /v1/faxes/forward` | Live testing confirmed the successful `200` response and the `404` response for an unknown source fax. Other validation errors, permissions, rate limits, and idempotency behavior remain undocumented. | The public reference uses the captured success and not-found responses. | Confirm remaining validation errors and operational limits. |
| `POST /v1/faxes/status` | The verified response returns `fax_call_start` and `fax_call_end` as Unix timestamps, while the API documentation standard requires ISO 8601 timestamps. Validation and error responses, permissions, rate limits, and polling guidance remain undocumented. | The public schema reflects the observed integer timestamps so it matches the live response. | Ask engineering to return ISO 8601 timestamps and confirm recommended polling intervals and terminal status values. |
| `POST /v1/faxes/resend` | Live requests confirmed success for a cancelled fax and two `400` cases: delivered faxes cannot be resent, and an unknown or account-inaccessible `job_id` returns “Fax not found or does not belong to your account.” Contrary to the earlier description, the successful response reused the original `job_id`. | The public reference uses the captured success and failure responses and states that the original `job_id` is reused. | Confirm permissions, rate limits, and idempotency behavior. |
| `POST /v1/faxes/lists` | A wrong JSON type for `sort_by` returned HTTP `500` instead of a client-error status. `sent_fax_time` is a Unix timestamp rather than ISO 8601. Request-side cursor fields, additional sort values, and whether the body is optional remain unconfirmed. Pending records omit `sent_pages` and `failed_pages`. | The public reference reflects the verified response shape, treats page-result fields as optional, and documents only the confirmed `Newest` sort example. | Return `400` or `422` for invalid input, convert timestamps to ISO 8601, and confirm supported sorting and pagination request fields. |
| `GET /v1/faxes/schedule` | Live testing confirmed populated and empty `200` responses. The `data` property is omitted when the list is empty. `created_at` and `scheduled_time` are Unix timestamps rather than ISO 8601. An invalid string `limit` was silently coerced to `1` and still returned `200`. | The public reference makes `data` optional and documents the timestamps as currently implemented. | Return ISO 8601 timestamps and reject invalid pagination values with a client error. |
| `POST /v1/faxes/cancel-schedule` | Live testing confirmed `200` success and `404` for an unknown fax. | The public reference uses the captured responses and marks `job_id` as required. | Confirm behavior for already transmitted or already cancelled scheduled faxes. |
| `POST /v1/faxes/cancel` | Live testing confirmed `200` success and `400` for an unknown fax; a supplied sample also confirms `400` when the fax is already cancelled. The success body unexpectedly returns `status: 0`. | The public reference preserves the observed responses and warns readers to use the HTTP status and message. | Change successful cancellation to return `status: 1`, or confirm that `status: 0` is intentional. |
| `POST /v1/faxes/received` | The confirmed response returns `duration` and `pages` as strings, `received_time` as a Unix timestamp, and phone numbers without the leading `+`. The supplied `marked_as` example contains a phone number, which conflicts with its apparent field name and the earlier `Not Done` example. | The public reference preserves the confirmed request and response exactly and marks all three request fields as required. | Return numeric duration/page values, use ISO 8601 for `received_time`, return E.164 phone numbers, and confirm the intended meaning and allowed values of `marked_as`. |
| `GET /v1/faxes/all/received` | The successful response uses numeric duration/page values, formatted date strings, and E.164 phone numbers, which conflicts with `POST /v1/faxes/received`. `received_time` is not ISO 8601. An invalid token produces HTTP `500` while the response body reports status `422`. | The public reference preserves the captured response types and error status. | Standardize both received-fax list schemas, use ISO 8601 timestamps, and return `401` for invalid authentication. |
| `POST /v1/faxes/pending-approval` | A populated `200` response was supplied. Its message contains spelling and capitalization errors: “Pending Approval Fax Rerive SuccessFully.” The timestamps are Unix values rather than ISO 8601. An empty `200` response omits both `message` and `data`; invalid `sort_by` returns `422`. | The public reference preserves both confirmed success shapes, makes `message` and `data` optional, and reflects the observed timestamp types. | Correct the success message, use ISO 8601 timestamps, and confirm all supported `sort_by` values. |
| `POST /v1/faxes/approval` | The `type` parameter accepts `approve` and `reject`, but its name does not clearly communicate that it represents a decision. An approved `200`, already-approved `400`, and unknown-fax `404` response are confirmed; a successful rejection response has not been captured. | The public reference documents the current `type` field, confirmed enum, and captured responses without exposing a proposed breaking rename. | Consider renaming `type` to `decision` in a future version and capture the successful rejection response. |
| `POST /v1/faxes/download` | The success message is grammatically incorrect: “The fax download successfully.” The API embeds the complete PDF in a Base64 data URL. | The public reference preserves the observed message but abbreviates the Base64 content. | Correct the success message and confirm maximum response size. |
| `POST /v1/faxes/mark-as-done` | The success response calls the fax a “Document,” while the not-found response inconsistently capitalizes “Fax.” | The public reference preserves the captured messages. | Standardize resource naming and message capitalization. |
| `POST /v1/faxes/mark-as-not-done` | The success message uses the nonstandard phrase “un-done,” while the not-found response inconsistently capitalizes “Fax.” | The public reference preserves the captured messages. | Use “marked as not done” and standardize capitalization. |
| `POST /v1/faxes/transmission-receipt` | The not-found message contains spelling and punctuation errors: “Fax don`t exits.” The response embeds the complete PDF in a Base64 data URL. | The public reference preserves the observed response and abbreviates Base64 content. | Correct the not-found message and confirm maximum response size. |
| `POST /v1/report` | Requests with a valid-looking date range and with an empty body both returned HTTP `404` containing only an opaque encrypted `data` string. No successful contract could be established. | The operation remains in OpenAPI with `x-excluded: true`, but its Mintlify page is hidden and removed from public navigation until the endpoint works. | Confirm the correct route, request format, and readable error contract, then capture a success response before publishing it. |
| `DELETE /v1/faxes/delete` | `job_id` is required. Confirmed `200` and `404` response samples are documented. | The operation is marked validated and both captured responses are public. | None for the confirmed contract. |
| `POST /v1/faxes/assign` | `job_id` and `emails` are confirmed as required, and multiple users can be assigned in one API call. A successful `200` response and the existing `400 No valid users found` response are documented. | The operation is marked validated and both captured responses are public. | None for the confirmed contract. |
| `POST /v1/faxes/unassign` | `job_id` and `emails` are confirmed as required, and multiple users can be unassigned in one API call. A successful `200` response and the existing `404 User not found` response are documented. | The operation is marked validated and both captured responses are public. | None for the confirmed contract. |

## OCR and NLP

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `POST /v1/ai/process` | The required `direction` field accepts `inbound` and `outbound`. The required `type` parameter accepts only the case-sensitive values `OCR` and `NLP`, although engineering would prefer to remove this requirement. Both `200` responses are confirmed, but the OCR response incorrectly says “Fax NLP request submitted successfully.” A failure response has not been captured. | The public contract preserves both observed responses and warns about the incorrect OCR message. | Remove the `type` requirement in a future API revision, correct the OCR success message, and capture a representative failure response. |
| `POST /v1/ai/extract` | `job_id`, `direction`, and the case-sensitive `type` field are required. `direction` accepts `inbound` and `outbound`. Complete NLP and OCR `200` responses were captured, and their `data.json_text` shapes differ. A failure response is still missing. | Postman preserves both complete responses. Mintlify presents shortened examples for both shapes and documents their structural differences in a dedicated guide. | Capture a representative failure response. |

## Labels

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `POST /v1/labels/assign` | The confirmed success message, “Fax label has updated.”, is grammatically incorrect. | The public example preserves the live response exactly. | Change the message to “Fax labels updated successfully.” |
| `POST /v1/labels/remove` | The confirmed success message, “Fax label has removed.”, is grammatically incorrect. | The public example preserves the live response exactly. | Change the message to “Fax labels removed successfully.” |

## Custom fields

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `POST /v1/custom/field/list` | A valid-looking nonexistent `job_id` returned `200` and exposed the same custom-field record observed for the supplied fax. | The public reference documents only the valid-fax response and the confirmed missing-`job_id` validation error. | Validate that the fax exists and belongs to the authenticated account before returning custom fields. |
| `POST /v1/custom/field/save` | A valid-looking nonexistent `job_id` returned `200` and accepted the custom-field update. Each item requires `id`; `value` is required when adding or updating a value. | The public reference documents the confirmed field requirements and captured success and validation responses. | Validate fax existence and ownership. |
| `POST /v1/custom/field/remove` | Removing a field returned success but left the field visible with `value: null`. A valid-looking nonexistent `job_id` also returned `200`. | The public reference describes the observed behavior as clearing a field value rather than guaranteeing deletion. | Validate fax existence and ownership, and confirm whether removed field records should remain visible. |

## Automations

All Automation operations are currently retained in OpenAPI and Postman but marked `x-excluded: true` and removed from public Mintlify navigation pending product and contract review.

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| `GET /v1/automations/list` | The response returns `last_run_at`, `created_at`, and `updated_at` as Unix timestamps instead of the documented ISO 8601 standard. A malformed bearer token returns HTTP `500`, while its body reports `status: 422` and `UNPROCESSABLE_ENTITY`. | The public example reflects the observed timestamp types. The malformed-token behavior is not advertised as an authentication contract. | Return ISO 8601 timestamps and use `401` for an invalid or malformed bearer token. |
| `POST /v1/automations/segments` | `trigger_type` is confirmed required, but the complete allowed trigger values are not yet confirmed. A missing field returns an opaque encrypted validation payload. One response category is misspelled as “Patient Indomograchics.” | The public reference marks `trigger_type` required and uses a shortened structural response example. | Publish the trigger enum, return readable validation errors, and correct the category label. |
| `POST /v1/automations/detail` | A nonexistent automation ID returns HTTP `200` with `status: 1` and “Automation not found.” Timestamps are Unix values, and empty action settings use inconsistent empty strings and arrays. | The public reference documents the observed not-found response under HTTP `200` and uses a shortened success example. | Return `404` with `status: 0`, use ISO 8601 timestamps, and normalize empty-value types. |
| `POST /v1/automations/create` | The source example uses apparently unrelated lead fields (`lead_created`, `country`, and `status`) and integer boolean values. No safe delete or cleanup endpoint is available, so a live creation test would leave persistent workspace data. Required fields, allowed values, and responses remain unconfirmed. | The endpoint remains marked pending and its Mintlify page warns that it has not been live-validated. | Supply a confirmed fax-automation payload and cleanup procedure, or authorize creation of a persistent test automation. |

## Numbers

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| Country, area, availability, search, filled requirements, and owned-number lists | Successful live responses were captured. `country_code` is required for area and filled-requirement requests. Both `country_code` and the area `code` are required when listing available numbers; `number` is required for search. Unsupported country/area values return HTTP `200`, `status: 1`, and omit `data`. An unavailable number also returns HTTP `200` with `status: 0`. Malformed-number validation returns opaque encrypted data. | These six operations are marked validated. OpenAPI now defines every response object and Postman contains the captured success, empty-result, unavailable-result, and validation examples. | Return readable validation errors and consider a clearer status contract for unsupported countries, areas, and unavailable numbers. |
| `POST /v1/numbers/filled_requirements` | This operation was previously designated internal-only, but it remains present in the approved customer Postman collection. | The contract is retained and now reflects its live response; its intended visibility still needs reconciliation. | Confirm whether this endpoint should remain public. |
| Number verification operations | Empty requests confirmed `country_code`, `file_name`, and `number_id` as required first-level fields. Create address and document upload now have confirmed `200` and `422` examples. Upload accepts `jpg`, `jpeg`, `png`, `pdf`, `doc`, and `docx`; remaining upload constraints are unconfirmed. Submit DID requirements creates a persistent compliance record. | The three operations remain partially validated. Create address and document upload have confirmed success schemas and validation examples; the DID-submission success envelope remains provisional. | Confirm all required address fields and remaining upload constraints such as file-size limits and whether validation uses the filename extension, media type, or file contents. Validate DID submission with a disposable verification order and approved test data. |
| Purchase and delete operations | `POST /v1/numbers/purchase` returns the same `422` availability-conflict message when `number` omits the leading `+`, even though the actual problem is invalid E.164 formatting. The message is also grammatically incorrect. Its optional `type` field is reported to accept `fax` and `voice`, but those values require developer confirmation. Delete is confirmed to remove the number from the Amplify account and deprovision it from the carrier. | Purchase and delete are validated with confirmed success and failure responses. Delete requires both identifiers, and its public page includes an explicit destructive-action warning. | Confirm the accepted purchase `type` values; return a distinct, accurate validation error for a malformed number; and improve the availability-conflict wording. |
| `POST /v1/numbers/purchase_with_requirements` | The available example uses top-level `type: fax` and requirement item types `textual`, `document`, and `address`, but the complete accepted enum and the validation rules for each evidence type have not been confirmed. | The success response is aligned with `POST /v1/numbers/purchase`. The observed type values are documented without inventing additional values. | Confirm the complete enums for the top-level `type` and `requirements[].type` fields, and document the required `data` and `other_data` format for each requirement type. |

## Contacts

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| Contact groups and contact custom fields | Complete create, list, update, and delete lifecycles were validated with temporary records, which were removed successfully. Create/update responses do not return the new resource ID, requiring a list lookup before subsequent operations. Several success messages have inconsistent capitalization. | All eight operations are marked validated with captured success and required-field responses. | Return the created/updated resource and standardize response messages. |
| Contact list and details | Contact phone values are not consistently E.164. The response contains the misspelled property `is_accesible_to_others`. A nonexistent contact returns HTTP `200` with `status: 1` and “Contact not found.” | The observed contract is documented; the not-found result remains under HTTP `200`. | Normalize phone numbers, correct the property in a versioned change, and return `404` with `status: 0` for missing contacts. |
| Contact create and update | One create succeeded with a disposable contact, which was located through List contacts and deleted. A second create returned the same HTTP `200` success message but no new contact appeared in the subsequent list. Empty create/update requests return HTTP `422` with opaque encrypted data. Update also returned the same opaque `422` for both a partial payload and a fuller profile payload, so its actual required-field contract remains unknown. | Create remains partially validated because its complete required-field set and false-positive success behavior are unknown. Update remains partially validated and retains its source success example, but the live success path could not be reproduced. Public examples use ISO dates and E.164 phone numbers. | Investigate the create false-positive response, return readable field-level validation errors, confirm the complete required fields, and provide a known-valid update payload. |
| Patient upload, save, and NLP matching | Upload is reachable and confirms `file_name` as required, but a successful call requires creating an orphaned document because no cleanup operation is available. Patient save and NLP matching returned HTTP `404` with opaque encrypted data for the source examples. | Upload remains partially validated with its confirmed `422`. Patient save and NLP matching remain in OpenAPI/Postman but are excluded from public navigation. | Confirm whether the two `404` routes are customer-facing, provide their live paths, and provide disposable NLP/document fixtures plus a cleanup procedure. |

## Documents and Templates

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| Collection list operations | Both list routes return HTTP `200`, but the complete payload is an opaque encrypted string in `data`; no document or template IDs can be derived. | Existing source-derived schemas remain pending because the live response cannot be compared with them. | Confirm whether customer clients must decrypt these payloads or change the API to return documented JSON. |
| Resource GET operations | Requests with placeholder IDs return `400` or `404` containing only opaque encrypted data. `GET /v1/esign_templates/{templateId}` with a string ID returns HTTP `500` and exposes a Laravel exception, server path, and stack trace. | No opaque error is promoted as a public schema. All affected operations remain pending. | Return readable structured errors, validate path parameters, and remove exception/stack-trace disclosure. |
| Create, update, send, upload, and delete operations | No valid disposable document/template IDs could be obtained. Mutations could create, send, or destroy customer records. | Operations remain documented from the existing source material but are explicitly unvalidated. | Provide disposable Collect fixtures and a cleanup workflow, then validate each resource lifecycle. |

## Engage API

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| Engage group inventory | The Basecamp group is named “Connect API,” while the current product name is Engage. | Public navigation uses **Engage API**. | Rename the Basecamp group when convenient. |
| Engage group inventory | The group contains Numbers endpoints, cron jobs, callbacks, and OpenSearch implementation tickets. | These are not automatically published as Engage endpoints. | Confirm whether any internal jobs need separate internal documentation. |
| `POST /guest_message/send` | The example JSON omits a comma after `content`; a later comment adds `is_secure` without defining its type or behavior. | The malformed example will be corrected; `is_secure` will be documented only after its contract is confirmed. | Confirm `is_secure` type, default, and effect. |
| `POST /guest_message/conversation/delete` | Response is supplied only as an image. | Method, path, and request can be documented; response remains unspecified until the image is reviewed. | Confirm the response status and schema. |
| Engage timestamps | Some examples use SQL-style timestamps while others use ISO 8601. | Public documentation follows the project convention of ISO 8601 UTC. | Confirm implementation normalization. |

## Collect API

| Endpoint or area | Issue | Documentation treatment | Follow-up |
| --- | --- | --- | --- |
| Collect group inventory | The Basecamp group is named “eSign API,” while the current product name is Collect. | Public navigation uses **Collect API**. | Rename the Basecamp group when convenient. |

## Cross-section inventory review

| Section | Conflicting or missing information | Sources involved | Documentation treatment | Follow-up |
| --- | --- | --- | --- | --- |
| Engage API | Basecamp provides 63 tickets but does not consistently identify public versus internal routes. The group also contains Numbers routes, cron jobs, callbacks, and OpenSearch tasks. | Basecamp “Connect API” group and existing Engage navigation plan. | Customer-facing Engage paths were published after user approval; misplaced and non-request routes remain internal. | Confirm individual visibility if an endpoint needs to differ from the approved family-level rule. |
| Collect API | Basecamp provides 182 tickets without a reliable public/internal designation. Some routes are UI-specific integrations, AI credit operations, or account-branding operations. | Basecamp “eSign API” group and existing Collect navigation plan. | Customer-facing Collect paths were published; AI credit, branding, and unrelated routes remain internal. | Confirm any additional Collect subresources that should be public. |
| Fax API | The 458-ticket group mixes core fax operations with account management, admin operations, OAuth callbacks, provider services, reports, and routes owned by other sections. | Basecamp “Fax Module” group and existing Fax/Numbers/Common navigation. | Genuine Fax, Folder, Label, Broadcast, and Automation paths were published; cross-section and infrastructure routes remain internal. | Confirm ownership of any overlapping route that should move sections. |
| Common APIs | The 52-ticket group includes Contacts, reseller administration, reports, roles, users, and utility routes. | Basecamp “Common Module APIs” group and existing standalone Contacts/Numbers sections. | 50 method-bearing contracts are staged internally; public Contacts pages remain the established source for duplicated contact operations. | Confirm whether reseller and client-administration APIs are public product APIs. |
| Background services | Cron, AI Session, and FreeSWITCH groups contain 70 additional tickets, primarily scheduled or service-to-service behavior. | Basecamp “Crons,” “Ai session,” and “Freeswitch” groups. | Reviewed as implementation material but not converted into public endpoint pages. | Create separate internal service documentation if engineering needs it. |

## Publication gate

The `Customer API v1` Postman collection is now the publication allowlist. It contains 115 customer-facing requests across Authentication, Send Fax, Numbers, Receive Fax, OCR & NLP, Contacts, Labels, Custom Field, Automations, Customer eSign Documents, and Customer eSign Templates. All other captured endpoint files remain available internally but are excluded from public navigation.

The public navigation currently references 102 existing endpoint pages that map to the approved collection. Thirteen approved Postman requests do not yet have a one-to-one Mintlify page or conflict with an older method/path contract; they should be authored or reconciled from the Postman request details before being added publicly. The known gaps are concentrated in Fax Report, several customer-specific Fax/Label/Custom Field operations, Numbers variants, Contacts detail/delete operations, and customer Automation routes.
