---
title: "DELETE /roles/{role_id}"
description: "Captured contract for DELETE /roles/{role_id}."
api: "DELETE /roles/{role_id}"
groups: ["internal"]
---

This internal review page records the behavior captured for DELETE /roles/{role_id}.

<ParamField header="access_token" type="string" required>API access token.</ParamField>

## Captured contract

```text
Use: Delete role.




Endpoint:

DELETE: api/roles/6093




Response:

image.png
```

<Note>Unspecified permissions, rate limits, idempotency, and error behavior remain under review.</Note>

